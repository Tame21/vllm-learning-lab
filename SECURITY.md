# 安全范围与反馈

本工具用于本机学习，默认仅监听 `127.0.0.1`。它不是公网托管服务，不包含用户认证或多人隔离。启动命令分析器只解析文本，不执行模型命令。

请使用当前版本，并保留源码服务的目录边界、隐藏文件过滤和符号链接检查。参考源码通过固定提交获取，不运行其安装脚本。

反馈普通缺陷时请在仓库创建 Issue，提供经过脱敏的复现步骤、系统与 Node 版本、参考源码提交及预期结果。不要粘贴令牌、私人目录、完整日志或真实业务提示词。

如问题涉及秘密泄漏或越界文件访问，请不要在公开 Issue 中上传真实数据或可直接利用的私人环境细节。可使用仓库启用的 GitHub 私密安全报告渠道；若该入口未启用，先提交不含敏感细节的联系请求，由维护者安排私下复现。

数据保存与命令显示边界详见 [隐私说明](docs/PRIVACY.md)。
