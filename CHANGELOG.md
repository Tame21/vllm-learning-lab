# 1.4.0

- 支持 `git clone` 后直接 `npm start`：自动获取固定提交的官方 vLLM 参考源码，不依赖同级目录、GPU、Python 或模型权重。
- 源码缓存在忽略目录中；支持离线复用和只读的 `VLLM_SOURCE_DIR`，不覆盖现有修改或版本不匹配的缓存。
- 清理本机路径示例，统一跨平台启动入口，补齐安装排错、隐私、安全、贡献、许可证与第三方说明。
- 增加针对实际 Git 暂存内容的发布检查，拦截生成物、私人路径和常见凭据特征。
- 增加首次准备、离线复用、失败清理、源码保护、完整启动与源码服务测试；CI 从 Windows / Linux 的干净检出验证 Node.js 22 / 24。

验证：53 项 Node 测试通过；官方固定提交首次下载、离线复用与本机服务启动均通过；66 个发布文件通过暂存区检查。参考源码与构建产物不进入发布仓库。

# 1.3.0

- 新增“投机推理方法地图”与 `#spec-methods` 路由，左侧固定快捷区、04 分组和学习路线均提供入口。
- 按草稿来源拆为 12 个方法专题，保留接受 / 拒绝可推演实验；动态长度、自适应验证和训练接入作为进阶专题。
- 各方法提供 4 步专属图解、输入与权重说明、源码边界、理解题和准确的代码跳转。整体扩展到 70 个专题、289 个显式定位步骤。
- 支持按来源筛选、双方法对照、配置模板带入命令流程图；模板选择与本章一致的 Runner，模型名明确为占位符。
- 区分 PARD 配置、EAGLE3 特征开关、MTP 模型分支、Medusa 线性候选、DFlash / DSpark 查询布局与 Markov 依赖。MLP 注册与 Runner 未接入，标为机制参考且不提供启动模板。
- 注册表命名映射支持精确定位到被注释的模型注册行；旧专题 ID 与既有理解题选项顺序保留。

验证：46 项 Node 测试通过；新增 3 组浏览器回归通过；390px 方法地图及专题图解无横向溢出，控制台无错误。本轮未运行真实模型，方法图是机制示意而非 GPU 执行记录。

# 1.2.0

- 新增“启动命令”视图及 `#command` 路由：解析启动参数，绘制服务启动与单个请求的逻辑处理链。
- 63 个参数规则、6 个示例，支持短别名、JSON / 点字段、关闭开关、常见续行及 Runner 环境变量。
- 主链速览、参数依据、条件分支、逐环节导览；点击节点跳到本地文件的实际函数行，56 个源码锚点在构建时校验。
- 区分启用能力与请求触发条件，处理 Pooling、Headless、内部 / 外部 DP 路由；未建模参数和外部配置明确提示范围。
- 保留 textarea 的实时值，独立工具视图支持刷新和历史导航；自定义命令不写入存储或 URL。

验证：40 项 Node 测试通过，包含新增的 14 项命令测试；9 组浏览器回归通过，包含新增的 4 组命令回归。另验证历史前后导航、390px 布局无横向溢出及无控制台错误。

范围：静态推导不执行命令，不读取模型或硬件状态，不替代完整启动校验；实际请求特征与平台分支明确标注。

# 1.1.0

本轮按源码定位、交互、状态准确性、实验深度、学习反馈、工程结构的顺序完成优化。

- 257 个步骤使用显式函数 / 章节锚点，支持函数范围、MRV1/MRV2 对照与源码变更待复核提示。
- 保留 DOM 控件节点与输入焦点；同参数回放复用执行记录。
- 修复重算标签、缓存终态和结构化输出的语法状态；执行图、事件与指标共用执行记录。
- 增加自定义请求、相同工作负载对照、可复现拒绝采样、TP 数值归并、PP 时间线、MoE 路由与组合。
- 62 道机制理解题、9 个专题的参数化挑战；学习路线、待复习、收藏、断点与实验链接。
- 作者源码移至 `src/`，构建生成 `dist/`；拆分引擎、显示、学习状态和可选代理接口，整理代码格式。

验证：26 项 Node 测试通过；5 组浏览器回归通过；另检查实验链接、前后导航、收藏、390px 布局与控制台。浏览器独立执行入口需要可选 Playwright 开发依赖，本轮通过 Codex 浏览器运行相同回归步骤。

范围：新增实验依然使用教学模型；不包含真实 GPU trace、生产调度器全部分支或所有第三方 kernel 的实现。
